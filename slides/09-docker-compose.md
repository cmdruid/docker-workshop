---
marp: true
---

# Docker Compose

Bring a composed stack up or down.
```bash
docker compose up
docker compose down
```

Common flags used with docker compose.
```bash
docker compose up --build -d
```